export type Dictionary = {
  header: {
    highlights: string;
    projects: string;
  };
  hero: {
    introducing: string;
    title: string;
    "title-highlight": string;
    subtitle: string;
    actionButton: string;
  };
};
