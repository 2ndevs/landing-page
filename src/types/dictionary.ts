export type Dictionary = {
  header: {
    highlights: string;
    projects: string;
  };
  hero: {
    introducing: string;
    title: string;
    subtitle: string;
    actionButton: string;
  };
};
